name: Fetch VRoid Female Base

on:
  push:
    branches: [aina-identity-master-v1-source]
    paths:
      - .github/workflows/fetch_vroid_base.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fetch:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout VRM sample repository
        uses: actions/checkout@v4
        with:
          repository: madjin/vrm-samples
          path: samples
          fetch-depth: 1
          lfs: true
      - name: Collect female VRoid base
        run: |
          set -euo pipefail
          mkdir -p output
          cp samples/vroid/fem_vroid.vrm output/fem_vroid.vrm
          cp samples/README.md output/LICENSE_README.md
          test -s output/fem_vroid.vrm
          ls -lh output
      - name: Upload sample
        uses: actions/upload-artifact@v4
        with:
          name: VROID-FEMALE-BASE
          path: output/**
          if-no-files-found: error
          retention-days: 7
