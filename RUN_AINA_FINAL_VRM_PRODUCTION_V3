Run the final hardened real AINA build, verify 52/52 ARKit controls, 18/18 VRM presets, required humanoid bones, real hair spring bones and clean Blender reimport through an exact-byte GLB alias, then publish a fixed candidate release with actual reference, expression and full-character review sheets. Do not set visual or production locks before those renders are inspected.
